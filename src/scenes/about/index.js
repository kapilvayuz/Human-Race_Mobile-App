import React from 'react';
import {SafeAreaView} from 'react-native';
import {CustomText} from '../../styles/typo'
const AboutScreen = () => (
  <SafeAreaView>
    <CustomText>Screen: About</CustomText>
  </SafeAreaView>
);

export default AboutScreen;
